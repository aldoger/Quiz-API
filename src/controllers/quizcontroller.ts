//TODO make a quiz controller for adding quizez
import { Request, Response } from "express";
import { AddQuizRequest } from "../dto/quiz.dto"

export const addQuiz = async (req: Request<any, any, AddQuizRequest>, res: Response) => {
    try{
        
    }catch(e: unknown){

    }
}